<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.singaporetech.hr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HR">
        <activity android:name=".SignActivity"
            android:label="@string/signInPageHeader"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/homePageHeader"
            android:exported="true"/>
        <activity
            android:name=".PayslipActivity"
            android:label="@string/payslipHeader"
            android:exported="true"/>
        <activity
            android:name=".PayslipListActivity"
            android:label="@string/payslipViewAll"
            android:exported="true"/>
        <activity
            android:name=".PayslipDetailActivity"
            android:label="@string/payslipViewDetails"
            android:exported="true"/>
        <activity
            android:name=".ConsolidatedPaySlipActivity"
            android:label="@string/payslipConsolidatedHeader"
            android:exported="true"/>
    </application>

</manifest>